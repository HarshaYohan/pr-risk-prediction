# Active Learning Plan

Begin with a labelled seed set, train a baseline model, score the unlabelled pool, select batches using random, least-confidence, entropy, margin, and diversity strategies, then merge human labels into the labelled pool.
