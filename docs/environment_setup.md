# Environment Setup

Use either `requirements.txt` with `venv` or `environment.yml` with conda. Run `pytest` after installation. Use Colab for early GPU experiments and AWS later for reproducible larger runs.
